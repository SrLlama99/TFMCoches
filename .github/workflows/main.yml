# .github/workflows/ping-url.yml

name: Update repo

on:
  push:
    branches:
      - main

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Visit URL
        run: |
          curl -X GET "https://aloxoblack.com/updateRepo"
