<nav class="customnav navbar navbar-expand-md m-1 p-2" data-bs-theme="dark">
    <div class="container">
        <span class="navbar-brand">
        <a href="{{ path('home')}}">
            <img class="navBarLogo" draggable="false" src="{{ asset('logo.png') }}" />
        </a>
        </span>
        <button class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div id="navbarCollapse" class="collapse navbar-collapse">
           <ul class="navbar-nav gap-3 w-100 flex-row justify-content-center my-2 my-md-0">
            <li class="nav-item position-relative">
               <form id="navForm" method="POST"> {# TODO: Change to search path here #}
                    <input type="hidden" value="{{ path('search_virpath') }}" name="queryEndpoint">
                    <input type="text" id="navFormQuery" name="query" class="navSearchBar form-control" placeholder="Search for a model..."/>
               </form>
                <div id="navFormSuggestionPanel" class="customnav searchDropdown position-absolute w-100" style="display: none;"></div>
            </li>

            {% if not app.user %} {# ANONYMOUS USER #}
            <li class="nav-item ms-md-auto">
                <a class="h-100 d-flex justify-content-center align-items-center gap-1 nav-link logIOButton customNavBtn btn" title="Log in" href="{{ path('app_login') }}">
                    <i class="bi bi-box-arrow-in-right px-2 px-md-0"></i>
                    <span class="d-none d-md-inline">Login</span>
                </a>
            </li>
            {% else %} {# USER IS LOGGED IN #}
            <li class="nav-item ms-md-auto">
                <a class="h-100 d-flex justify-content-center align-items-center gap-1 customNavBtn btn createNew" title="Create" href="{{ path('new') }}">
                    <i class="bi bi-plus-circle"></i>
                    <span class="d-none d-xl-inline">Create</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="h-100 d-flex justify-content-center align-items-center gap-1 garageButton customNavBtn btn" title="My garage" href="{{ path('garage', {'name':app.user.UserName}) }}">
                    <i class="bi bi-car-front-fill"></i>
                    <span class="d-none d-xl-inline">Garage</span>
                </a>
            </li>
            <li class="nav-item">
                <a class="logIOButton customNavBtn btn" title="Log out" href="{{ path('app_logout') }}">
                    <i class="bi bi-box-arrow-right"></i>
                </a>
            </li>
            {% endif %}
            </ul>
        </div>
    </div>
</nav>