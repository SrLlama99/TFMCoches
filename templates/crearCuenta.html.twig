<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crear Cuenta</title>
    <link rel="stylesheet" href="{{ asset('styles/login.css') }}">
</head>
<body>
<input type="checkbox" id="crearCuenta" class="hidden-checkbox" checked />
<div class="container2">
    

    <div class="right2">
        <div class="login-box">
            <img src="{{ asset('/images/logo.png') }}" class="logo-img">
            <a href="{{ path('app_login') }}">Go back to login -></a>
            <form method="post" action="{{ path('procesaCuentaFinal') }}">
            <label>Enter the code we sended to your mail:</label>
            <input type="text" name="codeValidation">
            {% if loginError == 1 %}
                <mark>The code doesn't match</mark>
            {% endif %}
            <input class="boton-login" type="submit" value="Create Account">
        </form>

        </div>
    </div>
    <div class="left2">
        <img src="{{ asset('/images/cars2.png') }}" class="cars-img2">
    </div>
</div>
</body>
</html>
